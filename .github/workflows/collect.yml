name: Container-Collector
run-name: ${{ github.actor }} is Collecting Images 🚀
on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * 1" # Update weekly on Monday Midnight
  workflow_dispatch:
    
jobs:
  Date:
    runs-on: ubuntu-latest
    outputs:
      Date: ${{ steps.date.outputs.date }}
    steps:
      - name: Get current date
        id: DATE
        run: |
          echo "DATE=$(date '+%Y%m%d-%H%M')" >> $GITHUB_OUTPUT
          echo -e "\n$GITHUB_OUTPUT\n"
          cat $GITHUB_OUTPUT

  Container-Images:
    needs: [Date]
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      
      - name: Set ROOTDIR
        id: vars
        run: |
          echo "ROOTDIR=Container-Images"
          echo "ROOTDIR=Container-Images" >> $GITHUB_OUTPUT

      - name: Run Collection Script for Container-Images.txt
        run: ./collect.sh ${{ steps.vars.outputs.ROOTDIR }}.txt

      - name: Install mkisofs
        run: |
          sudo apt-get update -y
          sudo apt-get install -y mkisofs
      
      - name: Make ISO
        run: |
          echo -e "\nMaking ISO of ${{ steps.vars.outputs.ROOTDIR }}"
          mkisofs -J -R -v -T -l \
            -V '${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}' \
            -A '${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}' \
            -o ${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}.iso \
            ${{ steps.vars.outputs.ROOTDIR }}

      - name: Show ISO
        run: ls -alh *.iso
        
      - name: Send ISO to Sharepoint
        uses: JacobsFederal/sharepoint-file-upload-action@pappasjfed-patch-1
        with:
          file_path: "*.iso"
          host_name: 'jacobsfederal.sharepoint.us'
          site_name: 'USCMSNISSCII.DSO'
          upload_path: '${{ vars.SHAREPOINT_BASEPATH }}/${{ vars.COLLECTOR }}/'
          tenant_id: ${{ secrets.SHAREPOINT_TENANT_ID }}
          client_id: ${{ secrets.SHAREPOINT_APP_ID }}
          client_secret: ${{ secrets.SHAREPOINT_APP_SECRET }}
          login_endpoint: 'login.microsoftonline.us'
          graph_endpoint: 'graph.microsoft.us'

  Atlassian-Images:
    needs: [Date]
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Set ROOTDIR
        id: vars
        run: |
          echo "ROOTDIR=Atlassian-Images"
          echo "ROOTDIR=Atlassian-Images" >> $GITHUB_OUTPUT

      - name: Run Collection Script for Atlassian-Images.txt
        run: ./collect.sh ${{ steps.vars.outputs.ROOTDIR }}.txt

      - name: Install mkisofs
        run: |
          sudo apt-get update -y
          sudo apt-get install -y mkisofs
      
      - name: Make ISO
        run: |
          echo -e "\nMaking ISO of ${{ steps.vars.outputs.ROOTDIR }}"
          mkisofs -J -R -v -T -l \
            -V '${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}' \
            -A '${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}' \
            -o ${{ steps.vars.outputs.ROOTDIR }}-${{ needs.Date.outputs.Date }}.iso \
            ${{ steps.vars.outputs.ROOTDIR }}

      - name: Show ISO
        run: ls -alh *.iso
        
      - name: Send ISO to Sharepoint
        uses: JacobsFederal/sharepoint-file-upload-action@pappasjfed-patch-1
        with:
          file_path: "*.iso"
          host_name: 'jacobsfederal.sharepoint.us'
          site_name: 'USCMSNISSCII.DSO'
          upload_path: '${{ vars.SHAREPOINT_BASEPATH }}/${{ vars.COLLECTOR }}/'
          tenant_id: ${{ secrets.SHAREPOINT_TENANT_ID }}
          client_id: ${{ secrets.SHAREPOINT_APP_ID }}
          client_secret: ${{ secrets.SHAREPOINT_APP_SECRET }}
          login_endpoint: 'login.microsoftonline.us'
          graph_endpoint: 'graph.microsoft.us'